[project]
name = "qwen"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "gradio>=5.12.0",
    "ipykernel>=6.29.5",
    "mlx-lm>=0.21.0",
]

[project.scripts]
llm = "llm:run"
